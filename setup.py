from setuptools import setup

setup(name='sobi',
      version='0.1',
      description='second-order blind identification',
      author='David Rigie',
      license='MIT',
      packages=['sobi'],
      zip_safe=False)